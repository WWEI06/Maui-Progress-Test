<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProgressTestMAD.MainPage"
             BackgroundColor="#1C1C1E">
    <ScrollView>
        <Grid Padding="16" RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Stroke="#E6B35A" StrokeThickness="3" Padding="3" BackgroundColor="Transparent">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="100" />
                    </Border.StrokeShape>
                    <Grid WidthRequest="80" HeightRequest="80">
                        <Image x:Name="ProfileImage" Source="vincent.jpg" Aspect="AspectFill" WidthRequest="80" HeightRequest="80">
                            <Image.Clip>
                                <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40" />
                            </Image.Clip>
                        </Image>
                    </Grid>
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                    <Label Text="Vincent" TextColor="#FFFFFF" FontSize="28" FontAttributes="Bold" />
                    <Label Text="Dutch, 1853 — 1890" TextColor="#C7C7CC" FontSize="14" />
                </VerticalStackLayout>
            </Grid>

            <Grid Grid.Row="1" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" x:Name="IntroCollapsed" Text="Was a Dutch Post‑Impressionist painter who is among the most famous and influential..." TextColor="#E5E5EA" LineBreakMode="TailTruncation" MaxLines="2" />
                <Label Grid.Column="1" Text="Read More" TextColor="#E6B35A">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnReadMoreTapped" />
                    </Label.GestureRecognizers>
                </Label>
            </Grid>
            <Label Grid.Row="2" x:Name="IntroExpanded" IsVisible="False" Text="Was a Dutch Post‑Impressionist painter who is among the most famous and influential figures in the history of Western art. In just over a decade he created about 2,100 artworks, including around 860 oil paintings." TextColor="#E5E5EA" />

            <Grid Grid.Row="3" ColumnSpacing="0" Padding="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <VerticalStackLayout Spacing="2">
                    <Label Text="$7.5m" TextColor="#FFFFFF" FontAttributes="Bold" />
                    <Label Text="Record" TextColor="#8E8E93" />
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="$3.5m" TextColor="#FFFFFF" FontAttributes="Bold" />
                    <Label Text="Average" TextColor="#8E8E93" />
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Column="2" Spacing="2">
                    <Label Text="+598%" TextColor="#FFFFFF" FontAttributes="Bold" />
                    <Label Text="General" TextColor="#8E8E93" />
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Column="3" Spacing="2">
                    <Label Text="12" TextColor="#FFFFFF" FontAttributes="Bold" />
                    <Label Text="Items" TextColor="#8E8E93" />
                </VerticalStackLayout>
            </Grid>

            <Label Grid.Row="4" Text="Other Works" TextColor="#FFFFFF" FontAttributes="Bold" FontSize="18" />

            <CollectionView Grid.Row="5" ItemsSource="{Binding Artworks}" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid RowSpacing="6">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Border Stroke="#3A3A3C" StrokeThickness="1">
                                <Image Source="{Binding Image}" Aspect="AspectFill" HeightRequest="160" />
                            </Border>
                            <VerticalStackLayout Grid.Row="1" Spacing="2">
                                <Label Text="{Binding Title}" TextColor="#FFFFFF" />
                                <Label Text="{Binding Price}" TextColor="#8E8E93" FontSize="12" />
                            </VerticalStackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ScrollView>
</ContentPage>
